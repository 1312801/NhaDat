<h1>New message to <%= @receiver.name %></h1>

<%= form_for @message do |f| %>
  <%= hidden_field_tag 'receiver_id', @receiver.id %>

  <%= f.label :body %>
  <%= f.text_area :content %>

  <%= f.submit %>
  <%= f.file_field :image %>
<% end %>
